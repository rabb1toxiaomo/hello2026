# 发布说明

## 📦 创建发布包

使用 `create-release.sh` 脚本创建发布压缩包。

### 使用方法

```bash
# 使用默认版本号 v1.0.0
./create-release.sh

# 指定版本号
./create-release.sh v1.0.1
```

### 输出文件

脚本会在 `release/` 目录下创建以下文件：

- `hello2026-{版本号}.zip` - ZIP 压缩包
- `hello2026-{版本号}.7z` - 7Z 压缩包（如果系统安装了 7z）
- `hello2026-{版本号}.zip.md5` - ZIP 文件的 MD5 校验和
- `hello2026-{版本号}.zip.sha256` - ZIP 文件的 SHA-256 校验和
- `hello2026-{版本号}.7z.md5` - 7Z 文件的 MD5 校验和（如果存在）
- `hello2026-{版本号}.7z.sha256` - 7Z 文件的 SHA-256 校验和（如果存在）

### 系统要求

#### ZIP 支持
- macOS/Linux: 系统自带 `zip` 命令
- Windows: 需要安装 Git Bash 或使用 WSL

#### 7Z 支持（可选）
- macOS: `brew install p7zip`
- Linux: `sudo apt-get install p7zip-full` 或 `sudo yum install p7zip`
- Windows: 下载安装 [7-Zip](https://www.7-zip.org/)

### 校验和验证

下载文件后，可以使用以下命令验证文件完整性：

#### macOS/Linux

```bash
# 验证 MD5
md5sum -c hello2026-v1.0.0.zip.md5

# 验证 SHA-256
sha256sum -c hello2026-v1.0.0.zip.sha256
```

#### macOS (使用 md5 和 shasum)

```bash
# 验证 MD5
md5 -c hello2026-v1.0.0.zip.md5

# 验证 SHA-256
shasum -a 256 -c hello2026-v1.0.0.zip.sha256
```

#### Windows (使用 PowerShell)

```powershell
# 验证 MD5
Get-FileHash hello2026-v1.0.0.zip -Algorithm MD5

# 验证 SHA-256
Get-FileHash hello2026-v1.0.0.zip -Algorithm SHA256
```

### 排除的文件

发布包中不包含以下文件：
- `node_modules/` - 依赖包（需要运行 `npm install` 安装）
- `.git/` - Git 仓库
- `*.db` - 数据库文件
- `.env` - 环境变量文件
- `logs/` - 日志文件
- `.vscode/` - VSCode 配置
- 其他临时文件和构建产物

### 使用发布包

1. **解压文件**
   ```bash
   unzip hello2026-v1.0.0.zip
   # 或
   7z x hello2026-v1.0.0.7z
   ```

2. **安装依赖**
   ```bash
   cd hello2026-v1.0.0
   npm install
   ```

3. **配置环境变量**
   ```bash
   cp .env.example .env
   # 编辑 .env 文件，设置管理员密码等
   ```

4. **启动服务**
   ```bash
   npm start
   ```

### 发布到 GitHub

1. **创建 Release**
   - 在 GitHub 仓库页面，点击 "Releases"
   - 点击 "Create a new release"
   - 填写版本号和发布说明

2. **上传文件**
   - 上传 ZIP 和 7Z 压缩包
   - 上传所有校验和文件（.md5 和 .sha256）

3. **发布说明模板**
   ```markdown
   ## 版本 v1.0.0
   
   ### 下载
   - [hello2026-v1.0.0.zip](下载链接)
   - [hello2026-v1.0.0.7z](下载链接) (可选)
   
   ### 校验和
   
   **ZIP 文件:**
   - MD5: `c9291aff13161a77d7220f4742e4cb83`
   - SHA-256: `6bd715b25fdc48cc70ef97b4df96e3d9a493d6aa4bbd45680cbb335179a6e1d1`
   
   **7Z 文件:** (如果生成)
   - MD5: `{7Z文件的MD5值}`
   - SHA-256: `{7Z文件的SHA256值}`
   
   ### 更新内容
   - 初始发布版本
   - 完整功能实现
   - 所有核心功能已完成
   ```

---

*最后更新: 2025-12-31*

